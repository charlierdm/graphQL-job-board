type Query {
  company(id: ID!): Company
  job(id: ID!): Job
  jobs: [Job!]
}

type Mutation {
  createJob(title: String!, companyId: ID!, description: String): Job
}

type Company {
  id: ID!
  description: String
  name: String!
  jobs: [Job!]
}

type Job {
  id: ID!
  company: Company!
  description: String
  title: String!
}